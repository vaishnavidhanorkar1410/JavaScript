var factorialOfNum = function (num){
    if(num==0){
        console.log(`Factorial of `);
    }
    let factorial = 1;
        for (let index = num; index >=1; index--)
        factorial = factorial * index;
        console.log( `Factorial of 5 is:${ factorial}`);
    
    }
    factorialOfNum(5);
    
    var factorialOfNum = function (num){
        if(num==0){
            console.log(`Factorial of `);
        }
        let factorial = 1;
            for (let index = num; index >=1; index--)
            factorial = factorial * index;
            console.log( `Factorial of 3 is:${ factorial}`);
        
        }
    factorialOfNum(3);
    
    var factorialOfNum = function (num){
        if(num==0){
            console.log(`Factorial of `);
        }
        let factorial = 1;
            for (let index = num; index >=1; index--)
            factorial = factorial * index;
            console.log( `Factorial of null is:${ factorial}`);
        
        }
    factorialOfNum(null);
    
    
    var factorialOfNum = function (num){
        if(num==0){
            console.log(`Factorial of `);
        }
        let factorial = 1;
            for (let index = num; index >=1; index--)
            factorial = factorial * index;
            console.log( `Factorial of 8 is:${ factorial}`);
        
        }
    factorialOfNum(8);
    
    
    var factorialOfNum = function (num){
        if(num==0){
            console.log(`Factorial of `);
        }
        let factorial = 1;
            for (let index = num; index >=1; index--)
            factorial = factorial * index;
            console.log( `Factorial of undefined is:${ factorial}`);
        
        }
    factorialOfNum(undefined);
    
    
    var factorialOfNum = function (num){
        if(num==0){
            console.log(`Factorial of 9`);
        }
        let factorial = 1;
            for (let index = num; index >=1; index--)
            factorial = factorial * index;
            console.log( `Factorial of 9 is:${ factorial}`);
        
        }
    factorialOfNum(9);
    
    var factorialOfNum = function (num){
        if(num==0){
        }
        let factorial = 1;
            for (let index = num; index >=1; index--)
            factorial = factorial * index;
            console.log( `Factorial of 0 is:${ factorial}`);
        
        }
    factorialOfNum(0);